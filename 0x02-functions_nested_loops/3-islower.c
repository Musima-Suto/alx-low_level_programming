#include "main.h"
/**
 *_islower- check for lowercase letter
 *Return:1 if lowercase, 0 if not.
 *@c: character to test
 */
int _islower(int c)
{
	int r = 'c';

	if (islower(r))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
